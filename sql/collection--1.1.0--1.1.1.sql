-- complain if script is sourced in psql, rather than via CREATE EXTENSION
\echo Use "ALTER EXTENSION collection UPDATE TO '1.1.1'" to load this file. \quit

-- No schema changes in this version bump
